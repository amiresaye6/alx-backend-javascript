// to compile the ts code t js >> tsc file.ts newfilename.js
// or tsc file.ts >> which will creat file.js
// you can ad the -w to comp
interface Student {
  firstName: string,
  lastName: string,
   age: number,
location: string,
}
